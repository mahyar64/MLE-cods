

/*import java.util.Timer;
import java.util.TimerTask;

final int interval = 1000*60*50;
        
        
		TimerTask run=new TimerTask (){
			@Override
			public void run() {
				// Toast.makeText(this, "wifi Button is enable", Toast.LENGTH_SHORT).show();
				//  detectWifi ();
				  /*	  detectGPS();
			      GPSpeed ();
			      GSMID ();
			      CurentTime ();
			      volume ();
			      vibration ();
			      mobiledata ();	
			
		};
    	new Timer().scheduleAtFixedRate(run ,10 ,interval); //set a timet for runing every 5 minut
        
 
      
			} 
			
			
    final int interval = 4000; // 1 Second
    Handler handler = new Handler();
    Runnable runnable = new Runnable(){
   	  @Override
   	  public void run() {
          // Toast.makeText(this, "C'Mom no hands!", Toast.LENGTH_SHORT).show();
   		  detectWifi ();
   		  GPSpeed ();
	      GSMID ();
	      CurentTime ();
	      volume ();
	      vibration ();
	      mobiledata ();	
	     
	       }
   };
   
   handler.postAtTime(runnable, System.currentTimeMillis()+interval);
   handler.postDelayed(runnable, interval);
   
   }
   */